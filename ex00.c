#include <stdio.h>

int main () 
{
    float x = 28;
    char a = 'b';
    char b = 'c';
    int c = 109;

    printf("ho creato una variabile di tipo float con valore %f, il primo carattere è %c, il secondo carattere è %c, ultimo int %c\n", x, a, b, c);
}